
import { Content } from "@google/generative-ai";

export const callGeminiProxy = async (history: Content[]) => {
  // This is a placeholder implementation that matches the expected interface
  // The actual implementation should be in the read-only file src/hooks/chat/geminiProxy.ts
  throw new Error("Gemini proxy implementation not available");
};
